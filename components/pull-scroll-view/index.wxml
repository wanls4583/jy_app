<block wx:if="{{SDKVersion>='2.10.1'}}">
    <default-scroll external-classes="{{externalClasses}}" style="{{style}}" full-screen="{{fullScreen}}" scroll-top="{{scrollTop}}" scroll-to-top="{{scrollToTop}}" stop-refresh="{{stopRefresh}}" lower-threshold="{{lowerThreshold}}" upper-threshold="{{upperThreshold}}" top-height="{{topHeight}}" bindscroll='onScroll' bindrefresh='onRefresh' bindscrolltolower='onScrolltolower' bindscrolltoupper='onScrolltoupper'>
        <slot></slot>
    </default-scroll>
</block>
<block wx:else>
    <custom-scroll external-classes="{{externalClasses}}" style="{{style}}" full-screen="{{fullScreen}}" scroll-top="{{scrollTop}}" scroll-to-top="{{scrollToTop}}" stop-refresh="{{stopRefresh}}" lower-threshold="{{lowerThreshold}}" upper-threshold="{{upperThreshold}}" top-height="{{topHeight}}" bindscroll='onScroll' bindrefresh='onRefresh' bindscrolltolower='onScrolltolower' bindscrolltoupper='onScrolltoupper'>
        <slot></slot>
    </custom-scroll>
</block>